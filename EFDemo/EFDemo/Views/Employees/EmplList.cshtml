@model IEnumerable<EFDemo.Models.Employee>
@{
    ViewBag.Title = "EmplList";
}

<h2>Employee List</h2>

<ul>
    @foreach (var item in Model)
    {
        <li> @Html.ActionLink(item.Name,"Details",new {id=item.Id}) </li>
    }
</ul>